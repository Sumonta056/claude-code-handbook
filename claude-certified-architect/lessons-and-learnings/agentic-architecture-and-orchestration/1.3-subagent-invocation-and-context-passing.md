# 1.3 Subagent invocation & context passing



Subagents are typically stateless and don't communicate directly with each other, subscribe to events, or share a memory store. Instead, a coordinator/orchestrator agent sits in the middle:

1. Coordinator invokes the web search subagent
2. Web search subagent returns its output to the coordinator
3. Coordinator synthesizes/selects the relevant findings and **includes them in the prompt/context** it constructs for the document analysis subagent
4. Coordinator invokes the document analysis subagent with that enriched prompt



* subagents generally don't call each other directly. Control flow runs through the coordinator, not agent-to-agent.
* subagents don't share a live API connection or session by default
* subagents typically don't have visibility into each other's conversation histories, and giving them fetch-tools

**he coordinator is the single point of context management**. Every subagent invocation should have its needed context deliberately included in the prompt by the coordinator — nothing flows automatically via events, shared memory, or direct agent-to-agent calls



3.4 The `Task` Tool for Spawning Subagents

Subagents are spawned via the `Task` tool:

```python
# The coordinator's allowedTools must include "Task"
coordinator_agent = AgentDefinition(
    allowed_tools=["Task", "get_customer"]
)
```

**Explicit context passing is mandatory:**

```
# Bad: the subagent has no context
Task: "Analyze the document"

# Good: full context in the prompt
Task: "Analyze the following document.
Document: [full document text]
Prior search results: [web search results]
Output format requirements: [schema]"
```

**Parallel spawning:** a coordinator can call multiple `Task`s in one response—subagents run in parallel:

```
# One coordinator response contains:
Task 1: "Search for articles about X"
Task 2: "Analyze document Y"
Task 3: "Search for articles about Z"
# All three run concurrently
```





> if the outputs genuinely overflowed the context window, the typical failure mode would be a truncation error, degraded/partial output, or an explicit length-related error

<figure><img src="../../../.gitbook/assets/image (12).png" alt=""><figcaption></figcaption></figure>

Why B works: passing the synthesis draft (3K tokens, already coherent) plus a **structured source index** (claim → URL/excerpt mapping) gives the report generation agent exactly what



<figure><img src="../../../.gitbook/assets/image (88).png" alt=""><figcaption></figcaption></figure>
